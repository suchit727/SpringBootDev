/*
package com.crossasyst.onetoone.annotations;

import com.crossasyst.onetoone.model.Person;
import jakarta.validation.ConstraintValidator;
import jakarta.validation.ConstraintValidatorContext;

public class NameValidator implements ConstraintValidator<BooleanValidator, Person> {
    @Override
    public boolean isValid(Person person, ConstraintValidatorContext constraintValidatorContext) {
        if (person.getIsNumberPresent()){
            return person.getNumber()!=null;
        }
        return true;
    }
}
*/
